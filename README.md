# ProjectDynamic
```
git init
git remote add origin https://github.com/Seulbi-Lee/ProjectDynamic.git
git config user.name "git 아이디"
git config user.email "git 이메일"
git branch -M main
git pull origin main
git push -uf origin main